import React from 'react'

import IntroSlider from './IntroSlider'

const Intro = ({ firstScreenSlider, playBtnText }) => {
  return (
    <IntroSlider firstScreenSlider={firstScreenSlider} playBtnText={playBtnText} />
  )
}

export default Intro